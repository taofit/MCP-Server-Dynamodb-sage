enabled: true
brokers:
  - localhost:9092
topics:
  heavy_ops: dynamodb-sage-heavy-ops
  notifications: dynamodb-sage-notifications
consumerGroupName: dynamodb-sage-workers
partitions: 3
dlq:
  topic: dynamodb-sage-dlq
  maxRetries: 3
  initialBackoff: 1s
  maxBackoff: 30s
  backoffMultiplier: 2.0